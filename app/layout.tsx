import './globals.css'
import NavBar from './components/nav-bar'

export const metadata = {
  title: 'Nextjs App',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html>
      <body>
        {/* ページ共通のコンポーネントを配置する */}
        <NavBar />
        {/* childrenにpagesの内容が渡される */}
        {children}
      </body>
    </html>
  )
}
